"""This module contains the action enum that holds all possible actions.
"""

from enum import Enum

class Actions(Enum):
    """Action enum that contains all possible actions."""
    NOTHING = 0
    PICKUP = 1
    DROPOFF = 2
